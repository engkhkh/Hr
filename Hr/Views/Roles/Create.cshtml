@model Hr.Models.Roles

@{
    ViewData["Title"] = "انشاء صلاحية ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1></h1>

<h4></h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @*<label asp-for="rolename" class="control-label"></label>*@
                اسم الصلاحية 
                <input asp-for="rolename" class="form-control" />
                <span asp-validation-for="rolename" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="حفظ " class="btn btn-success" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-success">عودة الي اسماء الصلاحيات </a>
</div>
@*<div>
    <a asp-action="Create" asp-controller="MenuModels" class="btn btn-success">عودة الي انشاء القائمة للصلاحية  </a>
</div>*@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
<script language="javascript" type="text/javascript">
        $(document).ready(function () {
            try {
                $("input[type='text']").each(function () {
                    $(this).attr("autocomplete", "off");
                });
            }
            catch (e) { }
        });

</script>