@model IEnumerable<Hr.Models.ACourcesTrainingMethod>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>عرض</title>
</head>
<body>

<table class="table table-bordered table-condensed table-striped table-hover sortable">
    <thead>
        <tr>
            <th></th>
            <th>
                @*@Html.DisplayNameFor(model => model.CourcesNameTraining)*@
            </th>

        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Create">اضافة </a>|
                <a asp-action="Edit" asp-route-id="@item.CourcesIdTraining">تعديل</a> |
                <a asp-action="Details" asp-route-id="@item.CourcesIdTraining">تفاصيل</a> |
                @*<a asp-action="Delete" asp-route-id="@item.CourcesIdTraining">Delete</a>*@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourcesNameTraining)
            </td>

        </tr>
}
    </tbody>
</table>
</body>
</html>
