@model IEnumerable<Hr.Models.ACourcesDeptin>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CourcesNameDeptin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourcesNameDeptin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourcesIdDeptin">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourcesIdDeptin">Details</a> |
                @*<a asp-action="Delete" asp-route-id="@item.CourcesIdDeptin">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
